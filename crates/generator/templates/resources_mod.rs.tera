//! Resource modules

{% for name in resource_names %}pub mod {{ name }};
{% if is_unified %}// Unified providers use module functions directly{% else %}pub use {{ name }}::{{ name | capitalize }};
{% endif %}{% endfor %}
